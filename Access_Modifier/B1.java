package Access_Modifier;
public class B1 {
    protected int n1=9;// a protected class ,method or data member can be accessed by classes within the same package and any subclass extends the class
     int marks=5; //if we have two file in same package then no need to declare variable or method with public
    
}
